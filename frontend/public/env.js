// Runtime configuration for the WorkLens frontend.
// Edit this file after deployment (no rebuild needed) if the API is not reachable
// at the default relative "/api" path — e.g. if the API is on a different host/port
// than the one nginx is proxying from.
//
// Example: window.__WORKLENS_API_BASE__ = "http://worklens-api.internal:8080/api";
window.__WORKLENS_API_BASE__ = '';
